# Hardware-in-the-Loop (HIL)

GridFlex command proposals remain advisory until HIL validation completes.

Confirm Zolt `proposeCommand` returns `proposal_only` with `executed: false` and readiness reports `physicalCommandExecutionEnabled: false`.
