# Immediate software sprint — 2026-08-12

Scope is **only** the ten items below. Physical HIL / plant / pen-test vendor /
POPIA IO sign-off are out of scope.

Physical execution stays locked: `PILOT_LOCK_PHYSICAL_EXECUTION=true`.

| # | Item | Software in this sprint | Operator / env still required |
|---|------|-------------------------|-------------------------------|
| 1 | Secure the KMS bootstrap script | Done — fail-closed spawn, dual-confirm access keys, confined output, `--self-test` | — |
| 2 | Freeze and sign a new RC | Tag `rc-2026-08-12` after merge; release workflow now signs `rc-*` tags | Wait for Cosign digest on the release run |
| 3 | Configure AWS KMS | Bootstrap + readiness scripts | `aws configure` + Render env |
| 4 | Rotate credentials | Rotation rehearsal script already exists | Staging/prod `aws_kms` vault |
| 5 | Deploy the exact digest to staging | `staging-rc-digest-deploy.md` + `verify:staging-digest` | Pin GHCR digest on Render (not Node rebuild) |
| 6 | Release-identity verification | `/api/health.release` + `npm run verify:staging-digest` | Set `RELEASE_GIT_SHA` / `RELEASE_IMAGE_DIGEST` |
| 7 | Current-RC restoration smoke | `restore:verify` + `smoke:restore-http` | Isolated Neon restore branch for this RC |
| 8 | Real logs and alerts | Dispatcher + fire-drill + env keys | Render log drain + `ALERT_WEBHOOK_*` + scrape token |
| 9 | Signed-ingest + WebSocket load | k6 ingest + `load:socketio` | Staging URL + load-test device credential |
| 10 | Reconcile readiness evidence | This file + evidence board | Fill SHA/digest rows after 2–9 complete |

## Freeze identity (fill after merge + sign)

| Field | Value |
|-------|-------|
| RC name | `RC-2026-08-12` |
| Git tag | `rc-2026-08-12` |
| Git commit SHA | _pending merge of this sprint PR_ |
| Signed image digest | _pending release workflow_ |
| Supersedes | `RC-2026-07-23` for **new** staging/prod deploys |

Do not deploy `d1a7363` or unsigned local builds.
